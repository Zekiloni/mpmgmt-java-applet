CREATE TABLE mobile_plans (ID BIGINT NOT NULL, CREATED_AT DATETIME, DATA_LIMIT INTEGER, DESCRIPTION VARCHAR(255), MINUTES_LIMIT INTEGER, MONTHLY_FEE DOUBLE, NAME VARCHAR(255) UNIQUE, PRIMARY KEY (ID))
CREATE TABLE customers (ID BIGINT NOT NULL, PRIMARY KEY (ID))
CREATE TABLE SUBSCRIPTION (ID BIGINT NOT NULL, PRIMARY KEY (ID))
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL(38), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
